# frozen_string_literal: true

RSpec.describe Glicko2 do
  it "has a version number" do
    expect(Glicko2::VERSION).not_to be_nil
  end

  it "does something useful" do
    # expect(false).to be(true)
  end
end
